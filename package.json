{
  "name": "dymension-bridge-sdk-monorepo",
  "private": true,
  "workspaces": [
    "sdk",
    "examples"
  ],
  "scripts": {
    "build": "npm run build -w sdk",
    "test": "npm run test -w sdk",
    "lint": "npm run lint -w sdk"
  }
}
